<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:aop="http://www.springframework.org/schema/aop"
	xmlns:context="http://www.springframework.org/schema/context" xmlns:p="http://www.springframework.org/schema/p"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="
                    http://www.springframework.org/schema/beans
                            http://www.springframework.org/schema/beans/spring-beans.xsd
                    http://www.springframework.org/schema/aop
                            http://www.springframework.org/schema/aop/spring-aop.xsd
                    http://www.springframework.org/schema/context
                            http://www.springframework.org/schema/context/spring-context.xsd
                    http://www.springframework.org/schema/mvc
                            http://www.springframework.org/schema/mvc/spring-mvc.xsd
            ">

	<description>
		Dieses ist die zentrale Konfiguration für das
		Spring-Servlet.
	</description>

	<!-- Schalte die Annotation für MVC ein -->
	<mvc:annotation-driven />

	<!-- Scanne das Package nach Spring Beans -->
	<context:component-scan base-package="com.pentasys.ws.java4web.web.impl" />

	<!-- Die Definition für den Aufruf der Index-Seite + JSPs -->
	<mvc:view-controller path="/" view-name="index" />
	<mvc:view-controller path="/users" view-name="users" />
	<mvc:view-controller path="/StartJSP" view-name="StartJSP" />
	<mvc:view-controller path="/ServletNoAOP"
		view-name="ServletNoAOP" />
	<mvc:view-controller path="/StartJavaBeanJSP"
		view-name="StartJavaBeanJSP" />

	<!-- Die Definition zum Mapping von Adressen auf Views -->
	<bean id="viewResolver"
		class="org.springframework.web.servlet.view.InternalResourceViewResolver"
		p:suffix=".jsp" p:prefix="/views/spring/"
		p:viewClass="org.springframework.web.servlet.view.JstlView" />

	<bean id="angular2ViewResolver" 
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix">
			<value>/views/angular2/</value>
		</property>
		<property name="suffix">
			<value>.html</value>
		</property>
	</bean>

	<!-- <bean id="handlerMapping" class="org.resthub.web.springmvc.router.RouterHandlerMapping"> 
		<property name="routeFiles"> <list> <value>routes.conf</value> Router will 
		*append* routes declared in additional files <value>addroutes.conf</value> 
		</list> </property> Uncomment the following configuration line if you want 
		routes to be dynamically reloaded when route files are modified. Can be a 
		good idea in dev mode, not so much in production! <property name="autoReloadEnabled" 
		value="true" /> </bean> -->

	<import resource="spring-aop.xml" />


</beans>

